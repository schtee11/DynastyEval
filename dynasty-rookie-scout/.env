# API keys are managed server-side — do not expose keys here
# Leave REACT_APP_API_URL empty to use the Netlify proxy (recommended)
# The Netlify proxy forwards /api/* to Railway, avoiding CORS issues
REACT_APP_API_URL=
